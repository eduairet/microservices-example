import { Asap, Azeret_Mono, Rubik_Mono_One } from 'next/font/google';

export const asap = Asap({
  variable: '--font-asap',
  subsets: ['latin'],
});

export const azeretMono = Azeret_Mono({
  variable: '--font-azeret-mono',
  subsets: ['latin'],
});

export const rubikMonoOne = Rubik_Mono_One({
  weight: '400',
  subsets: ['latin'],
});
